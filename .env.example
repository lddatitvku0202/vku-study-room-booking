# Environment template — copy to .env and fill in.
# .env is gitignored; this template is tracked. Never commit real credentials.
#
# No values are required at TASK 01 (scaffold only).
# Firebase configuration keys are added in TASK 04 / TASK 06.
